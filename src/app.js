import React from 'react'
import ReactDOM from 'react-dom'
import SearchBar from './components/search-bar'

const APP = React.createElement({
  return (
    <div>
      <SearchBar />
    </div>
  )
});

export deafault ReactDOM.render(document.getElementById("root"), <APP />)
